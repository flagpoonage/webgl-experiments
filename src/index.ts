// import { runTriangle } from './canvas/index';

import { runWebglCubeDemo } from './mdn-example-spin-cube/webgl-demo';
import { runRenderToTexture } from './r-to-t/index';

document.addEventListener('DOMContentLoaded', () => {
  runRenderToTexture();
  // runWebglCubeDemo();
});
``;
